# Where the Laravel API lives. Server-side only — this value is never shipped to the
# browser, because every API call is made from a Server Component or Server Action.
API_URL=http://localhost:8000

# Realtime (optional). Leave the key blank and the app simply does not push —
# threads still load on request. Values come from backend/.env after
# `php artisan reverb:install`.
NEXT_PUBLIC_REVERB_APP_KEY=
NEXT_PUBLIC_REVERB_HOST=localhost
NEXT_PUBLIC_REVERB_PORT=8080
NEXT_PUBLIC_REVERB_SCHEME=http
